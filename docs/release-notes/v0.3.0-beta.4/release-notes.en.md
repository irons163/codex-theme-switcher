# Codex Theme Switcher 0.3.0-beta.4

## Complete language refresh

- Rebuilds the full interface hierarchy when the selected language changes.
- Fixes stale labels in nested views, including the built-in theme badge.
- Preserves theme edits, selections, and runtime state during a language switch.
- Adds translation coverage for the built-in badge in all seven languages.
