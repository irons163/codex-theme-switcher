{
  "name": "codex-theme-switcher-runtime",
  "version": "0.3.1-beta.1",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node scripts/check-localization.js && node --test test/*.test.js",
    "check:localization": "node scripts/check-localization.js",
    "check": "find Sources/CodexThemeRuntime/Resources -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=22"
  }
}
